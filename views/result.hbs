    <div class="parentDiv">
        <h1 class="centreText">You're a...</h1>
    </div>

    <div class="resultDiv">
      <h1>{{name}}</h1>
      <img class="result_image" src="{{img}}">
      <p>Answers:</p>
      <ol>
        {{#each answers}}
          <li>
            {{this}}
          </li>
        {{/each}}
      </ol>
    </div>
